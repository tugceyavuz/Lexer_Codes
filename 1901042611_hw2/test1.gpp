;; This is example g++ code to test lexer
(load "filename")
(deffun example_function (param1 param2)
    (set sum (+ param1 param2))
    (set substract (- param1 param2))
    (set divide (/ param1 param2))
    (set product (* param1 param2))
    (set power (** param1 param2))
    (if (equal (equal param1 20) true)
        (disp "param1 is 20")
    )
    (if (equal (equal param1 20) false)
        (disp "param1 is not 20")
    )
    (if (not (equal param1 20))
        (disp "param1 is not 20")
    )
    (if (and (equal param1 20) (equal param2 20))
        (disp "param1 and param2 are 20")
    )
    (if (or (equal param1 20) (equal param2 20))
        (disp "param1 or param2 is 20")
    )
    (set liste1 nil)
    (append liste1 4)
    (set liste2 (list 1 2 3))
    (concat liste1 liste2)
    (set a 123f456)
    (set i 10)
    (for 
        (set i (- i 1))
        (if (less i 1)
            (exit)
        )
    )
)